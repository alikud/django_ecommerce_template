This is simple django-based playground.

I realise JWT Standard https://www.rfc-editor.org/rfc/rfc7519 in users app.
Now authentication_classes = (JWTAuthentication,) is provide custom logic for authentication
